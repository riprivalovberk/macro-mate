from typing import Optional

def verify_token(token: Optional[str] = None):
    # TODO: add token and token verification
    # if token != "secretToken":
    #     raise HTTPException(status_code=400, detail="Invalid token")
    return token
